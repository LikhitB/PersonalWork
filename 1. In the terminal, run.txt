1. In the terminal, run:

sudo -i

2. In the terminal, run:

sudo yum install -y amazon-efs-utils
sudo yum install -y python3-botocore

3. Go to the next step.

mkdir data
sudo mount -t efs -o tls fs-02434b07334c67969:/ efs
sudo bash -c "cat >> efs-1-setup.log"



CREATE SECURITY GROUP ALLOW NFS LINK WITH DEFAULT SECURITY GROUP
CREATE EFS 
DURING MOUNT ATTACH THE SEC GRP CREATED IN 1 STEP
ALL MUST BE IN THE SAME VPC
MOUNTING MUST BE DONE IN SESSION MANAGER BY PUTTING ABOVE COMMMANDS